<!DOCTYPE html>
<html lang="it">
    <head>
        <title>Verifica il tuo account</title>
        <meta charset="utf-8">
        <link rel="stylesheet" href="{{bootstrap_css}}">
        <link rel="stylesheet" href="{{jquery_ui_css}}">
        <link rel="stylesheet" href="../css/menu.css">
        <link rel="stylesheet" href="../css/verify.css">
        <script src="{{bootstrap_js}}"></script>
        <script src="{{jquery_js}}"></script>
        <script src="{{jquery_ui_js}}"></script>
        <script src="../js/verify.js"></script>
    </head>
    <body>
        {{> menu}}
        <div class="{{container}}">
            <div class="verifyDiv">
               <form id="fVerify" method="post" action="#">
                    <fieldset>
                        <legend>Verifica il tuo account</legend>
                        <div class="container-fluid">
                            <div class="row">
                                <div class="col-12 col-lg-2">
                                    <label for="activation-code" class="form-label">Codice di attivazione</label>
                                </div>
                                <div class="col-12 col-lg-10">
                                    <input type="text" id="activation_code" name="activation_code" class="form-control">
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-12 d-flex justify-content-center">
                                    <button type="submit" id="bVerify" class="btn btn-primary">VERIFICA</button>
                                </div>
                            </div>
                        </div>
                    </fieldset>
                </form> 
            </div>
        </div>
    </body>
</html>