<!DOCTYPE html>
<html lang="it">
    <head>
        <title>{{title}}</title>
        <meta charset="utf-8">
        {{#links.list}}
            {{&links.link}}
        {{/links.list}}
        {{#scripts.list}}
            {{&scripts.script}}
        {{/scripts.list}}
        {{^preferences}}
            {{> cookie_banner}}
        {{/preferences}}
    </head>
    <body>
        {{#username}}
            {{> menu_logged}}
        {{/username}}
        {{^username}}
            {{> menu}}
        {{/username}}
        <div class="my-container">
            {{&content}}
        </div>
        {{> footer}}
    </body>
</html>